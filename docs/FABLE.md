# Fable / future-agent start

Canonical engineering source: **https://github.com/MMGSaint/mortis-field-network** (`main`).

Do not unpack ZIP packs. Do not rebuild Phase 1. Do not invent canon. Do not open Phase 3. Do not Connect production Discord.

```
git clone https://github.com/MMGSaint/mortis-field-network.git
cd mortis-field-network
npm install
npm run typecheck
npm run test:engine
```

Then read [AGENT_HANDOFF.md](AGENT_HANDOFF.md). Invariants there are binding.

Least-privilege invite integer: **`295011699728`**. An earlier published `294851834304` was a transcription error. Re-invite if the scratch bot still uses it or holds Administrator.

Leave Discord Interactions Endpoint URL **blank** while the workstation gateway is the ACK path.

`docs/FABLE5_TAKEOVER_PROMPT.txt` and `artifacts/FABLE5_HANDOFF/` are historical operating notes. Where they conflict with this file or AGENT_HANDOFF, **this file and AGENT_HANDOFF win**.
