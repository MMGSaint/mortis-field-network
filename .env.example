# Never commit real values. Enter the bot token on Provision (memory only) in preview.
# For a deployed envoy Worker, use `wrangler secret put` instead of this file.

# DISCORD_BOT_TOKEN=
# DISCORD_APP_ID=1540058003888410806
# DISCORD_PUBLIC_KEY=
# CLI_SECRET=
# RELEASE_PUBLIC_KEY=
# DATABASE_URL=
