# INVARIANTS AND HARD STOPS

## Identity

Player-facing Discord name: **MORTIS FIELD NETWORK**.

Authority: VRChat = witness · Registry/app = verify · Discord = hear (least authoritative).

Players enter as Initiates. No lore dump. No progress meters.

Dual-register: obligations/safety in plain language.

## Never on player surfaces

Ashwright · R-grades · True Name · Saint’s Circle / DM-layer · Season 3 in new copy · FACT-/CON-/TRG-/MINE- · sprint/build/AI talk.

Use: Initiate · Shadow · notices / briefings / mission orders / reports · published pillar names at Initiate visible-work level.

Composed names need `approved:` or the `neutral_fallback`.

## Envoy stores

Members, staff, blueprint_state, templates, events (operational metadata + state machine), tickets, audit.

Never: canon facts, fragments, dossiers, reveal schedules, [BIND] names, unpublished material, signing private keys, relay secrets.

## Dispatch

One function: `dispatch.send` / `dispatchSend`. Eight steps. Block, never redact.

Staff-category posts skip steps 2–6 but still do 1, 7, and 8. Ticket channel posts must not land on player-facing blueprint channels.

## Provision

Match by blueprint key. No-drift re-apply is a no-op. Orphans reported. History archive-lock only. 400/403 on individual Discord mutations is non-fatal so remaining creates can finish.

## Permissions

Least privilege integer `294851834304`. `blueprint/guild.json` `never_administrator: true`. Channel member overwrite (type 1, bot user id) over guild-wide Admin.

## Interactions

Leave Discord Interactions Endpoint URL blank while using the workstation gateway. No privileged intents. ACK within 3s. Modal openers are type 9.

## Frozen

`workers/mortis-relay/**` · player apps · release-builder core · Admin Console v1 · `keys/` · PROP_*.json · canon export as envoy data.

## Owner-owned (do not answer)

Season 3 interpretive question · app branding · DM trigger entry point · identity linking · ⚠ composed-name stamps.
